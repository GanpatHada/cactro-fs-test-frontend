import React from 'react'

const PollCard = ({poll}) => {
  const {pollId,question,options}=poll;  
  return (
    <div className='poll-card'>
         <h2></h2>
    </div>
  )
}

export default PollCard
